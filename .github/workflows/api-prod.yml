name: API Prod release
on:
  workflow_dispatch:
jobs:
  build:
    name: Build RHEL
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup .NET Core SDK '8.0.x'
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '8.0.x'
      - name: Build
        run: dotnet publish -c Release -r linux-x64 API/PowerApps.csproj -p:PublishSingleFile=false --self-contained false
        
      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
           name: prod-build
           path: ./API/bin/Release/net8.0/linux-x64/publish/
           if-no-files-found: error
      
  deploy:
    name: Deploy to prod
    runs-on: ubuntu-latest
    needs: build
    
    steps:
    - name: Finish Deployment
      run: |
        api_url=$API_URL
        response=$(curl -s $api_url)
        echo "Response: $response"
        if [ "$response" != "Ok" ]; then
          exit 1
        fi
      env:
        API_URL: ${{ secrets.PROD_API_TRIGGER }}
    
